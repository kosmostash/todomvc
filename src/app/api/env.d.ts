export declare module "_/api" {
  interface DefaultVariables {
    /** Set by `requestLog` in api/use.ts, echoed back as the `x-request-id` header. */
    requestId: string;
  }
  interface DefaultBindings {}
}
